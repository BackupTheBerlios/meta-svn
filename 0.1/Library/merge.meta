documentation="Combines several maps into one."
|repeat
	array=@
	function=
		|
			overwrite=@.present
			&=repeat
				array=keys @.present
				function=
					|
						result=@.previous
						result.[@.present]=overwrite.[@.present]
						&=result
				start=@.previous
	start=*
|/.Meta.Library.Merge @