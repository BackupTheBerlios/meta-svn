documentation="Combines several maps into one."
|sum
	array=@
	function=
		|
			overwrite=@.present
			&=sum
				array=keys @.present
				function=
					|
						result=@.previous
						result.[@.present]=overwrite.[@.present]
						&=result
				start=@.previous
	start=*
|Meta.Library.Merge @