documentation="Calls a function with every value in an array as an argument and returns the results as an array."
|applyImplementation
	array=arg.array
	function=arg.function
	index=1
	result=*
applyImplementation=
	|
		stop=greater
			arg.index
			length arg.array
		next=
			=
				|arg.result
			[*]=
				|applyImplementation
					array=arg.array
					function=arg.function
					index=add
						arg.index
						1
					object=Meta.ObjectMap.[mscorlib.Object] arg.result
					argument=arg.function arg.array.[arg.index]
					object.Append argument
					result=object.This.get *
		this=next.[stop] arg