|
	result foreach
		. invert apply
			. string.split
				text ["INPUT.txt"]
				delimiters
					. characters.linefeed
			line|
				parts string.split
					text line
					delimiters
						. characters.tab
				current
					[parts.[2]] parts.[1]
		entry|sort
			. entry.value
			arg|string.compare
				. arg.[1]
				. arg.[2]
	sortedKeys sort
		. keys result
		arg|string.compare
			. arg.[1]
			. arg.[2]
	sortedOutput 0
	. apply
		. sortedKeys
		key|
			$sortedOutput.[key] result.[key]
	current sortedOutput