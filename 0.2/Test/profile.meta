|
	lines string.split ["INPUT.txt"]
		. characters.linefeed
	map invert (apply lines
		line|
			parts string.split line
				. characters.tab
			:
				[parts.[2]] parts.[1])
	sorted foreach map
		entry|sort entry.value string.compare
	: apply (sort (keys map) string.compare)
		key|prepend key sorted.[key]