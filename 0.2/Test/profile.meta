getLines
	text|string.split
		text text
		delimiters
			. characters.linefeed
getTabsMap
	text|
		parts string.split
			text text
			delimiters
				. characters.tab
		:
			[parts.[2]] parts.[1]
|
	lines getLines ["INPUT.txt"]
	map invert (apply lines
		line|getTabsMap line)
	sorted foreach
		. map
		entry|sort
			array entry.value
			function
				arg|string.compare
					. arg.[1]
					. arg.[2]
	: apply (sort
		array keys map
		function
			arg|string.compare
				. arg.[1]
				. arg.[2])
		key|join
			. 
				. key
			. sorted.[key]