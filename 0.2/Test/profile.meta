|
	lines string.split ["INPUT.txt"]
		. characters.linefeed
	map invert (apply lines
		line|
			parts string.split line
				. characters.tab
			:
				[parts.[2]] parts.[1])
	sorted foreach map
		entry|sort entry.value
			arg|string.compare arg.[1] arg.[2]
	: apply (sort (keys map)
		arg|string.compare arg.[1] arg.[2])
		key|prepend key sorted.[key]